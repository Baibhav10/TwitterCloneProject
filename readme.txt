 TO run justTweet code with the node:
1. Open two separate terminals.
2. In terminal 1, navigate to expressApp folder and run the server.js using prompt: node server
3. In terminal 2, run the App.js using prompt: npm start

Both of the terminal should simultaneously execute in order to connect mongoDb to the justTweet app.